const games=["Cricket","Hockey","Football","Volleyball","Tennis","Golf"]

      games.map((item)=>{
        console.log(item);
      })
        console.log(games.map((item)=>{
            return name
      }));
